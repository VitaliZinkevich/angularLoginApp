import { SpendedTimePipe } from './spended-time.pipe';

describe('SpendedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SpendedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
